# Copy this file to `.env` and fill in your key, then either
#   export $(cat .env | xargs)
# or use a tool like direnv / python-dotenv to load it.
#
# Get a key at https://console.anthropic.com/ -> API Keys.
ANTHROPIC_API_KEY=sk-ant-...
